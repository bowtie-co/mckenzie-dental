# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# gem "rails"

# Required on Ruby 3.4+ (no longer bundled; Jekyll and deps require these)
gem "base64"
gem "bigdecimal"
gem "csv"
gem "logger"
gem "openssl"
# For `jekyll serve` (built-in server removed from Ruby 3.0+)
gem "webrick"

# Liquid 4.0.4+ required for Ruby 3.2+ (tainted? was removed)
gem "liquid", ">= 4.0.4"

gem "jekyll" 
