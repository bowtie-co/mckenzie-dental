# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# gem "rails"

# Required on Ruby 3.4+ (no longer bundled with Ruby)
gem "base64"
gem "csv"
gem "logger"

gem "jekyll" 
